{
  "name": "mee-extension",
  "version": "1.0.0",
  "description": "",
  "main": "background.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build:safari && npm run build:chrome",
    "build:safari": "APP_BROWSER=safari vite build && APP_FILE=popup APP_BROWSER=safari vite build",
    "build:chrome": "vite build && APP_FILE=popup vite build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "terser": "^5.17.1",
    "vite": "^4.3.3",
    "vite-plugin-static-copy": "^0.14.0"
  }
}
